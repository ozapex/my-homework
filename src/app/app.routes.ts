import { Routes } from '@angular/router';
import { MaterialComponentComponent } from './material-module/material-component/material-component.component';

export const routes: Routes = [
    {path: 'jobNumber8', component:MaterialComponentComponent}
];
